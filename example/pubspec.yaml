name: example
description: A demo app for nimble_story_designer
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  nimble_story_designer:
    path: ../
